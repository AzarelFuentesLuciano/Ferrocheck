(function(){var element=document.querySelector('[data-auth-welcome]');if(!element)return;var text=element.getAttribute('data-text')||element.textContent||'';if(window.matchMedia('(prefers-reduced-motion: reduce)').matches){element.textContent=text;return;}var index=0,erasing=false;element.textContent='';function tick(){if(!erasing){index++;element.textContent=text.slice(0,index);if(index===text.length){erasing=true;window.setTimeout(tick,3000);return;}window.setTimeout(tick,110);return;}index--;element.textContent=text.slice(0,index);if(index===0){erasing=false;window.setTimeout(tick,700);return;}window.setTimeout(tick,75);}window.setTimeout(tick,400);})();
