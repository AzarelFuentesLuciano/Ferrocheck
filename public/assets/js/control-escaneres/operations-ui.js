document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-vo-operation-form]').forEach(form=>{form.addEventListener('submit',()=>{const button=form.querySelector('button[type="submit"],button:not([type])');if(!button||button.disabled)return;button.disabled=true;button.setAttribute('aria-busy','true');button.dataset.originalLabel=button.textContent;button.textContent=button.dataset.loadingLabel||'Registrando…';});});const error=document.querySelector('[role="alert"], [aria-invalid="true"]');if(error instanceof HTMLElement){error.tabIndex=-1;error.focus({preventScroll:false});}});
